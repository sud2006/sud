//iki sayıının toplamını bulunuz.
#include <stdio.h>

int main()
{
    int x,y;
    int toplam;
    printf("iki sayı giriniz:\n");
    scanf("%d%d",&x,&y);
    toplam=x+y;
    printf("toplam: %d+%d=%d",x,y,toplam);
    return 0;
}
